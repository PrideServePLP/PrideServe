NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only. Required by `npm run seed`; never expose this to the browser.
SUPABASE_SERVICE_ROLE_KEY=
# Optional: shared password for the seeded test accounts.
SEED_PASSWORD=
